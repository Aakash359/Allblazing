import { StyleSheet } from 'react-native';
import Constants from '../constants';

const styles = {
  amount: {
    flex: 1,
    flexDirection: 'column',
  },
  amountStyle: {
    ...Constants.Fonts.regularSmall,
    color: Constants.Colors.WHITE,
    textAlign: 'center',
  },
  animation: {
    height: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 60,
    width: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 60,
  },
  animationContainer: {
    alignItems: 'center',
    alignSelf: 'center',
    flex: 1,
    justifyContent: 'center',
  },
  animationStyle: {
    alignItems: 'center',
    alignSelf: 'center',
    backgroundColor: Constants.Colors.PRIMARY,
    borderRadius: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 40,
    justifyContent: 'center',
  },
  bubble: {
    alignItems: 'center',
    alignSelf: 'flex-start',
    backgroundColor: Constants.Colors.YELLOW,
    borderColor: Constants.Colors.YELLOW,
    borderRadius: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 10,
    borderWidth: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 0.5,
    flexDirection: 'row',
    justifyContent: 'center',
    paddingHorizontal: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
    paddingVertical: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
    width: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 35,
  },
  bubbleContainer: {
    alignItems: 'center',
    height: 55,
    justifyContent: 'center',
    width: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 35,
  },
  bubbleContent: {
    alignItems: 'center',
    alignSelf: 'flex-start',
    flexDirection: 'column',
    justifyContent: 'center',
  },
  button: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  distanceView: { marginTop: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 5 },
  filter: {
    backgroundColor: Constants.Colors.PRIMARY,
    borderRadius: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 8,
    bottom: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 8,
    height: 40,
    paddingHorizontal: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3,
    position: 'absolute',
    right: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 5,
  },
  filterContainer: {
    backgroundColor: Constants.Colors.TRANSLUCENT,
    bottom: 0,
    left: 0,
    position: 'absolute',
    right: 0,
    top: 0,
    zIndex: 1,
  },
  filterHeader: {
    ...Constants.Fonts.regularBold,
    margin: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3,
  },
  filterLeftContainer: {
    backgroundColor: Constants.Colors.TRANSPARENT,
    height: '100%',
    width: '10%',
  },
  filterRightContainer: {
    backgroundColor: Constants.Colors.WHITE,
    height: '100%',
    left: '0%',
  },
  filterWrapper: {
    backgroundColor: Constants.Colors.TRANSPARENT,
    flex: 1,
    flexDirection: 'row',
    width: '100%',
  },
  headerRightButton: { marginRight: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3 },
  headerSubTitle: {
    ...Constants.Fonts.regularSmall,
    color: Constants.Colors.SECONDARY_COLOR,
    textAlign: 'center',
  },
  headerTitle: {
    ...Constants.Fonts.regularBold,
    textAlign: 'center',
  },
  location: {
    backgroundColor: Constants.Colors.TRANSPARENT,
    bottom: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 22,
    position: 'absolute',
    right: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 5,
  },
  locationContainer: {
    alignItems: 'center',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    margin: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
  },
  locationTextStyle: {
    ...Constants.Fonts.regularBold,
    color: Constants.Colors.PRIMARY,
    marginLeft: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
    textAlign: 'left',
  },
  map: {
    backgroundColor: Constants.Colors.TRANSPARENT,
    flex: 1,
    height: Constants.BaseStyle.DEVICE_HEIGHT,
    width: Constants.BaseStyle.DEVICE_WIDTH,
  },
  mapStyle: {
    flex: 1,
    height: null,
    width: null,
  },
  mapViewStyle: { width: Constants.BaseStyle.DEVICE_WIDTH },
  marker: {
    height: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 10,
    width: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 10,
  },
  markerCustomView: {
    alignItems: 'center',
    alignSelf: 'center',
    backgroundColor: Constants.Colors.PRIMARY,
    borderColor: Constants.Colors.PRIMARY,
    borderRadius: 5,
    borderWidth: 5,
    height: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 10,
    justifyContent: 'center',
    width: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 10,
  },
  markerPrice: {
    ...Constants.Fonts.largeBold,
    color: Constants.Colors.WHITE,
  },
  markerSelectedCustomView: {
    alignItems: 'center',
    alignSelf: 'center',
    backgroundColor: Constants.Colors.PRIMARY,
    borderColor: Constants.Colors.YELLOW,
    borderRadius: 5,
    borderWidth: 5,
    height: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 10,
    justifyContent: 'center',
    width: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 10,
  },
  parkingContainer: {
    alignItems: 'flex-start',
    alignSelf: 'flex-start',
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'flex-start',
    marginHorizontal: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3,
  },
  parkingTextStyle: {
    ...Constants.Fonts.regularSmall,
    color: Constants.Colors.CHARCOAL,
    fontSize: 12,
    marginLeft: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 0.5,
    textAlign: 'left',
  },
  parkingView: {
    alignItems: 'center',
    backgroundColor: Constants.Colors.WHITE,
    borderColor: Constants.Colors.LIGHT_GRAY,
    borderRadius: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 5,
    borderWidth: 1,
    flexDirection: 'row',
    justifyContent: 'center',
    margin: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 1,
    paddingHorizontal: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
    paddingVertical: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
  },
  placeholderStyle: {
    height: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 33,
    width: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 40,
  },
  popover: {
    backgroundColor: Constants.Colors.WHITE,
    borderRadius: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
    bottom: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 8,
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
    left: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 5,
    padding: Constants.BaseStyle.PADDING,
    position: 'absolute',
    right: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 5,
  },
  popoverPriceWrapper: {
    alignItems: 'flex-end',
    alignSelf: 'flex-end',
    flex: 5,
  },
  popoverWrapper: { flex: 5 },
  priceContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  priceText: {
    ...Constants.Fonts.regularSmall,
    color: Constants.Colors.SECONDARY_COLOR,
    marginHorizontal: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3,
  },
  rateActiveText: { color: Constants.Colors.YELLOW },
  rateContainer: { marginHorizontal: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3 },
  rateSelected: { borderColor: Constants.Colors.YELLOW },
  rateText: {
    ...Constants.Fonts.regularBold,
    alignSelf: 'center',
    color: Constants.Colors.CHARCOAL,
    paddingHorizontal: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 1,
    textAlign: 'center',
  },
  rateWrapper: {
    alignItems: 'center',
    borderColor: Constants.Colors.BORDER_COLOR,
    borderRadius: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
    borderWidth: 1,
    flexDirection: 'row',
    justifyContent: 'center',
    marginHorizontal: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
    paddingHorizontal: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 2,
    paddingVertical: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 1,
  },
  restore: {
    alignItems: 'center',
    justifyContent: 'center',
    marginRight: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3,
  },
  restoreContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginVertical: (Constants.BaseStyle.DEVICE_HEIGHT / 100) * 1,
  },
  restoreText: {
    ...Constants.Fonts.regularSmall,
    alignSelf: 'center',
    textAlign: 'center',
  },
  search: {
    alignItems: 'center',
    alignSelf: 'center',
    justifyContent: 'center',
    marginBottom: (Constants.BaseStyle.DEVICE_HEIGHT / 100) * 4,
    marginTop: (Constants.BaseStyle.DEVICE_HEIGHT / 100) * 4,
    width: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 60,
  },
  searchWrapper: {
    backgroundColor: Constants.Colors.WHITE,
    margin: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3,
  },
  separator: {
    height: 1,
    marginLeft: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3,
    marginTop: (Constants.BaseStyle.DEVICE_HEIGHT / 100) * 1,
    width: Constants.BaseStyle.DEVICE_WIDTH,
  },
  serviceRow: { marginLeft: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 1.5 },
  slider: {
    alignItems: 'center',
    alignSelf: 'center',
    justifyContent: 'center',
    margin: (Constants.BaseStyle.DEVICE_WIDTH / 100) * 3,
  },
};

export default StyleSheet.create(styles);
